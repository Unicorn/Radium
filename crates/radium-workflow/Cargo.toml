[package]
name = "radium-workflow"
version.workspace = true
edition = "2021"
authors.workspace = true
description = "Workflow compiler and TypeScript generator for Radium"
license.workspace = true
repository.workspace = true

[[bin]]
name = "radium-workflow"
path = "src/main.rs"

[dependencies]
# Web framework
axum = { version = "0.7", features = ["matched-path"] }
tokio = { workspace = true }
tower = { workspace = true }
tower-http = { version = "0.5", features = ["cors", "trace", "timeout", "request-id", "limit"] }

# Serialization
serde = { workspace = true }
serde_json = { workspace = true }
serde_yaml = "0.9"

# Validation
validator = { version = "0.20", features = ["derive"] }

# Templating
handlebars = "5"

# Error handling
thiserror = { workspace = true }
anyhow = { workspace = true }

# Logging & Observability
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
opentelemetry = "0.22"
opentelemetry_sdk = { version = "0.22", features = ["rt-tokio"] }
opentelemetry-otlp = { version = "0.15", features = ["tonic"] }
tracing-opentelemetry = "0.23"

# Async utilities
futures = "0.3"

# gRPC client (Temporal)
tonic = "0.12"

# HTTP client
reqwest = { version = "0.12", features = ["json"] }

# UUID generation
uuid = { version = "1", features = ["v4", "serde"] }

# Date/time
chrono = { version = "0.4", features = ["serde"] }

# Regex for validation
regex = "1"

# Cryptographic hashing (API key authentication)
sha2 = "0.10"

# Sibling crates (for future integration)
# radium-models = { workspace = true }

[dev-dependencies]
pretty_assertions = "1"
tokio-test = "0.4"
tempfile = "3"
criterion = { version = "0.5", features = ["html_reports"] }
serial_test = "3"

[[bench]]
name = "compilation_benchmarks"
harness = false

[features]
default = []

[lints]
workspace = true
