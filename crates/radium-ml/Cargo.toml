[package]
name = "radium-ml"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
# Internal crates
radium-abstraction.workspace = true

# Async runtime
tokio.workspace = true

# Serialization
serde.workspace = true
serde_json.workspace = true

# Error handling
anyhow.workspace = true
thiserror.workspace = true

# Logging
tracing.workspace = true

# HTTP client (for Python bridge via HTTP)
reqwest = { workspace = true, features = ["json"], optional = true }

[features]
default = ["python-bridge"]

# Python bridge via subprocess (uses tokio::process::Command from modern tokio)
python-bridge = []

# Python bridge via HTTP server
http-bridge = ["reqwest"]

# ONNX runtime (future: pure Rust inference)
# onnx-runtime = ["ort"]
